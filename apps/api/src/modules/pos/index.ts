/**
 * pos — in-store counter checkout run by staff. Composes catalog + inventory +
 * orders + payments. Kept as its own module so an offline-capable POS could be
 * added later behind this boundary without a rewrite (see §11 Risks).
 */
export {};

/**
 * Admin Portal (Next.js on Vercel) — staff workspace: POS, inventory, billing,
 * returns/refunds, customers, fulfillment. Talks only to the API; holds no
 * business logic. Separate app from the customer site for security isolation (ADR-007).
 */
export default function AdminHome() {
  return <main>Shop Admin Portal — scaffold</main>;
}

/**
 * Customer Website (Next.js on Vercel) — storefront: browse catalog, cart,
 * checkout (Stripe or cash on delivery), order history, delivery status.
 * Talks only to the API.
 */
export default function StoreHome() {
  return <main>Shop — scaffold</main>;
}
